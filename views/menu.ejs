<!DOCTYPE html>
<html>
<head>
  <title>Manage Menu</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f9;
      margin: 0;
      padding: 20px;
    }
    h1 {
      text-align: center;
      color: #2c3e50;
    }
    a {
      display: inline-block;
      margin-bottom: 20px;
      text-decoration: none;
      color: white;
      background: #3498db;
      padding: 8px 14px;
      border-radius: 6px;
      transition: background 0.3s;
    }
    a:hover {
      background: #2980b9;
    }
    form {
      margin: 15px 0;
    }
    input, button {
      padding: 10px;
      margin: 5px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 14px;
    }
    button {
      background: #27ae60;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background: #219150;
    }
    ul {
      list-style: none;
      padding: 0;
      max-width: 600px;
      margin: auto;
    }
    li {
      background: white;
      padding: 12px;
      margin: 10px 0;
      border-radius: 8px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    li strong {
      color: #2c3e50;
    }
    li form {
      margin: 0;
    }
    li button {
      background: #e74c3c;
    }
    li button:hover {
      background: #c0392b;
    }
    hr {
      border: none;
      height: 2px;
      background: #ddd;
      margin: 20px 0;
    }
    h2 {
      color: #34495e;
    }
  </style>
</head>
<body>
  <h1>Manage Menu</h1>
  <a href="/admin/dashboard">← Back to Dashboard</a>
  <hr>

  <h2>Add New Item</h2>
  <form method="POST" action="/category/menu/add">
    <input type="text" name="name" placeholder="Item Name" required>
    <input type="number" name="price" placeholder="Price" required>
    <button type="submit">Add</button>
  </form>

  <h2>Current Menu</h2>
  <% if (menuItems.length === 0) { %>
    <p>No items in the menu.</p>
  <% } else { %>
    <ul>
      <% menuItems.forEach(item => { %>
        <li>
          <span><strong><%= item.name %></strong> - ₹<%= item.price %></span>
          <form method="POST" action="/category/menu/delete/<%= item._id %>" style="display:inline;">
            <button type="submit">Delete</button>
          </form>
        </li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
